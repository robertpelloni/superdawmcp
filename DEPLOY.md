# SuperDAW-Universal-MCP Deployment

## Configuring Bitwig Studio
1. Copy the Java adapter from `pkg/agents/bitwig/` to your Bitwig extensions folder.
2. Ensure the Bitwig MCP extension is enabled in Bitwig settings.
3. The default port is 8181.
